%div{:id => "slide#{index+1}", :class => classname}
  .project_table
    .project_screens.right
      .phones.parallax
        %div
          = image_tag picture.asset, :alt => "Picture"
    .project_text_right
      %h1= picture.title.capitalize
      %p= picture.description.capitalize