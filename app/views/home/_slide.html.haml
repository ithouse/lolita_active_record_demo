%div{:id => "slide#{index+1}", :class => (index == 0 ? "tabb displayslide" : "tabb displaynone")}
  .project_table
    .project_screens.right
      .phones.parallax
        %div
          = image_tag(slide.main, :alt => "Picture") if slide.main?
    .project_text_right
      %h1= slide.title
      %p= slide.description

